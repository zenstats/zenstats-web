# ZenStats 前端 API 迁移任务

将以下内容发送给另一个 Cline 对话：

---

请帮我完成以下 API 迁移和功能优化任务：

## 任务 1：将主图表从旧版 /curve 迁移到新版 /main-graph

**文件**: `src/pages/sites/stats/components/main-graph.tsx`

- 当前使用 `domain + "/curve"` 接口
- 改为使用 `domain + "/main-graph"` 接口
- 新接口返回格式：`{ timestamp: "2024-01-25", metrics: { visitors: 1500, pageviews: 3200 } }`
- 添加时间间隔选择器（Hourly/Daily/Weekly/Monthly）
- 根据选择的指标（visitors/pageviews）展示不同数据
- 支持 `metrics=visitors,pageviews` 参数

## 任务 2：将聚合指标从旧版 /top_stats 迁移到新版 /aggregate

**文件**: `src/pages/sites/stats/components/aggregate-stats.tsx`

- 当前使用 `domain + "/top_stats"` 接口
- 改为使用 `domain + "/aggregate"` 接口
- 新接口返回格式：`{ results: { visitors: { value: 12345, comparison_value: 10000, change: 23.45 }, ... } }`
- 使用新接口的 `comparison_value` 和 `change` 字段

## 任务 3：将分解表格从旧版 API 统一迁移到 /breakdown

**文件**: `src/pages/sites/stats/stats.tsx`

- 将 `getSourceRank`、`getPageRank`、`getDeviceRank` 替换为统一的 `getBreakdown` 接口
- 使用 `property=visit:source`、`property=event:page`、`property=visit:device` 等参数

## 任务 4：更新 Mock 数据

**文件**: `src/utils/mock.ts`

- 更新 mock 数据格式以匹配新 API 返回格式
- 确保 `/main-graph` 返回 `timestamp` + `metrics` 格式
- 确保 `/aggregate` 返回 `results` 格式

## 任务 5：更新接口类型定义

**文件**: `src/pages/sites/types/interfaces.ts`

- 确保 `MainGraphPoint` 类型匹配新 API 格式
- 确保 `AggregateResponse` 类型匹配新 API 格式

## 参考信息

后端 API 文档路径：`/Volumes/Workspace/Developer/golang/zenstats/docs/api-stats.md`

当前项目工作目录：`/Volumes/Workspace/Developer/web/zenstats-web`